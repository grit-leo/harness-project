You are verifying a POLISH sprint fix. DO NOT do a full site audit.

## SCOPE (STRICT — DO NOT DEVIATE)
Only verify these pages: __POLISH_PAGES__
Only test these journeys: __POLISH_JOURNEYS__

## CONTEXT
- Previous full review: __PREV_REVIEW_FILE__
- Polish contract: __POLISH_CONTRACT__
- Project root: project/

## RULES
1. Skip pages marked ✅ in the previous review — they didn't change.
2. Focus ONLY on pages that were ❌ or ⚠️ in the previous review AND are in the fix scope.
3. Do NOT test responsive behavior unless the fix is explicitly mobile-related.
4. Do NOT test performance, dark mode, or design system consistency unless the fix touches those.

## WORKFLOW (Max 15 steps)
1. Navigate to each fixed page → take screenshot → test the specific fix
2. Try the broken flow end-to-end (e.g., if Disputes loading was fixed, actually load data)
3. Check browser console for NEW errors introduced by the fix
4. Spot-check 1-2 adjacent pages for regressions

## SCORE (5 dimensions, max 10 points)
| Dimension | Weight | How to Evaluate |
|-----------|--------|-----------------|
| Fix Works | 40% | Did the reported bug actually get fixed? |
| No Regressions | 30% | Did the fix break anything nearby? |
| UX Polish | 15% | Is the fix clean, not hacky? |
| Edge Cases | 10% | Does it handle empty states / errors gracefully? |
| Speed | 5% | Is the interaction responsive (< 2s)? |

## OUTPUT
Write your verification to `__VERIFY_OUTPUT__`:

```markdown
# Polish Verification — Round __POLISH_NUM__

## Scope
Pages tested: __POLISH_PAGES__

## Fix Results
| # | Issue | Status | Evidence |
|---|-------|--------|----------|
| 1 | ... | ✅ FIXED / ❌ STILL BROKEN / ⚠️ PARTIAL | screenshot: ... |

## Regression Check
| Page | Status | Notes |
|------|--------|-------|
| ... | ✅ No regression | ... |

## Score: X.X / 10

## Verdict
SHIP_IT: true  ← if score >= 7.0 AND no P0 regressions
SHIP_IT: false ← if score < 7.0 OR any P0 regression
```

## CRITICAL
- Be HONEST. If the fix didn't work, say so.
- If you find a NEW bug caused by the fix, flag it as a REGRESSION.
- Do NOT pad the score to be nice. A "5" means the fix is incomplete.
